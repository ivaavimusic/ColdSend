# Server
PORT=4000
HOST=0.0.0.0
JSON_LIMIT=2mb
MAX_UPLOAD_BYTES=52428800

# Transfer adapter selection: 'wifi-direct' (default), 'bluetooth', 'mock'
TRANSFER_ADAPTER=wifi-direct

# Wi-Fi Direct adapter settings (optional)
WIFI_DISCOVERY_PORT=8888
WIFI_TRANSFER_PORT=8889

# Bluetooth (BLE) adapter settings (optional)
# Only needed if using TRANSFER_ADAPTER=bluetooth
# BLE_TARGET_NAME=MyBLEDevice
# BLE_SERVICE_UUID=ffe0
# BLE_CHARACTERISTIC_UUID=ffe1
# BLE_MTU=180
# BLE_SCAN_TIMEOUT_MS=10000
